"Use-Strict";
//? : thats called ternary operators
const grown = age >= 18 ? "Adult" : "Teen";
console.log(grown);

let grownTow;
if (age >= 18) {
  grownTow = "Adult";
} else {
  grownTow = "Teen";
}
console.log(grownTow);
